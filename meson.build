project (
    'unitube-gtk',
    ['c', 'vala'],
    version: '0.1.0'
)

app_id = 'com.nucleuxsoft.UniTube'

prefix = get_option ('prefix')
datadir = join_paths (prefix, get_option ('datadir'))

subdir ('data')
subdir ('src')
